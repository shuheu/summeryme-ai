---
description:
globs: backend/**/*
alwaysApply: false
---
# work directory

以下のディレクトリで作業すること

/backned

# stack

rails
docker-compose

# commmand

railsコマンドなど利用する際はdokcer container で実行すること

dokcer compose run --rm api bin/rails

docker compose run --rm api bundle install

## lint

check

docker compose run --rm api bundle exec rubocop

fix

docker compose run --rm api bundle exec rubocop -A

# github actions

github actionsはリポジトリのルートの.github ディレクトリを利用する