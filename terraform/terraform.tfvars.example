# Terraform Variables for Summeryme AI Backend
# このファイルをコピーして terraform.tfvars を作成し、実際の値を設定してください

# 基本設定
project_id  = "your-gcp-project-id"
region      = "asia-northeast1"
environment = "production"

# Cloud SQL設定
db_tier      = "db-f1-micro"  # 本番環境では db-n1-standard-1 などを検討
db_disk_size = 10             # 本番環境では 20GB以上を推奨

# Cloud Run設定
container_image = "your-region-docker.pkg.dev/your-project/your-repo/your-app:latest"
cpu_limit       = "1000m"    # 1 vCPU
memory_limit    = "1Gi"      # 1GB RAM
min_instances   = 0          # コスト最適化のため0
max_instances   = 1          # ハッカソン用：最大1インスタンス

# アプリケーション設定
log_level           = "info"  # debug, info, warn, error
allow_public_access = true    # パブリックアクセスを許可